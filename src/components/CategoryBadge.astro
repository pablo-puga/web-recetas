---
interface Props {
    name: string;
    count: number;
    highlight?: boolean;
}

const { name, count, highlight = false } = Astro.props;
---

<a
    href={`/categoria/${name.toLocaleLowerCase()}/pagina/1`}
    aria-current={highlight ? 'true' : 'false'}
>
    {name} - {count}
</a>

<style>
    a {
        @apply border border-theme-teal-600 rounded-full px-2 text-theme-teal-600 text-sm hover:bg-theme-teal-600 hover:text-theme-grey-900 transition-colors;
    }

    a[aria-current='true'] {
        @apply font-bold border-2 text-theme-teal-700 border-theme-teal-700 hover:bg-theme-teal-800 hover:border-theme-teal-800 hover:text-white;
    }
</style>
