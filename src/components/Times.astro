---
import clsx from 'clsx';
import { FaRegClock } from 'react-icons/fa6';

interface Props {
    prepTime: number;
    cookTime: number;
    className?: string;
}

const { prepTime, cookTime, className } = Astro.props;
---

<ul class={clsx('flex flex-row gap-3', className)}>
    <li class="border-r-2 border-theme-red-200 pr-3">
        <div>
            <span class="text-xl font-semibold">Tiempo de preparación</span>
            <span class="flex flex-row items-center gap-2">
                <FaRegClock className="text-theme-red-500" />
                {prepTime} min
            </span>
        </div>
    </li>
    <li>
        <div>
            <span class="text-xl font-semibold">Tiempo de cocinado</span>
            <span class="flex flex-row items-center gap-2">
                <FaRegClock className="text-theme-red-500" />
                {cookTime} min
            </span>
        </div>
    </li>
</ul>
